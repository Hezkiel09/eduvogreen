import 'package:flutter/material.dart';

class UnggahVolunteerPage extends StatelessWidget {
  const UnggahVolunteerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Unggah Volunteer"),
      ),
      body: const Center(
        child: Text("Halaman Unggah Volunteer"),
      ),
    );
  }
}